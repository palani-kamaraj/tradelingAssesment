export enum DropdownOptions {
  USERS = "users",
  REPO = "repo",
  ISSUES = "issues",
}

export enum APIMethod {
  GET = "GET",
  POST = "POST",
}

export enum Status {
  OPEN = "open",
  CLOSED = "closed",
}
